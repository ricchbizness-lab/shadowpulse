# Clés API ShadowPulse Pipeline
# Copier ce fichier en .env et renseigner les valeurs réelles.
# Ne jamais committer .env — il est dans .gitignore.

# Hunter.io — découverte d'emails professionnels
# https://hunter.io/api-keys
HUNTER_API_KEY=

# XposedOrNot — vérification de fuites de credentials (gratuit, sans clé)
# https://xposedornot.com/api — quota : 25 req/heure, 100 req/jour
# Aucune variable à renseigner, le module fonctionne sans configuration.
